# Copy to front/.env and fill in your value. Never commit front/.env.

# Base URL of the backend API.
# Must be your machine's LAN IP, NOT localhost — Expo Go runs on a physical
# device, so "localhost" would resolve to the phone itself.
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
